# Project-scoped Codex settings.
# Custom agents inherit the active parent model unless an agent file overrides it.

project_doc_max_bytes = 65536

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
